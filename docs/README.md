# BlockCell PDF Extractor - GitHub Pages

This directory contains the GitHub Pages deployment files for the BlockCell PDF Extractor website.

## Files Structure
- `index.html` - Main website page
- `styles.css` - Stylesheet (if separate)
- `script.js` - JavaScript functionality (if separate)

## Deployment
This site is automatically deployed via GitHub Pages from the `/docs` folder.
